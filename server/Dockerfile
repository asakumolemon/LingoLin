# Stage 1: 编译
FROM golang:1.26-alpine AS builder

WORKDIR /build
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 go build -o /build/lingolin-server ./cmd/server

# Stage 2: 运行
FROM alpine:3.21

RUN apk add --no-cache ca-certificates tzdata

WORKDIR /app
COPY --from=builder /build/lingolin-server .

EXPOSE 8080

VOLUME ["/app/data", "/app/store"]

ENTRYPOINT ["/app/lingolin-server"]
